"use strict";

// Strict Mode
// - this will forbid us to do certain things which could cause error in programm
// - and, it creates errors visible in console

// ex:1
let hasDriverLicence = false;
let passTest = true;

if (passTest) {
  hasDriverLicence = true;
} else {
  hasDriverLicence = false;
}

// ex:2
let interface = "raj";
let private = "raj";
